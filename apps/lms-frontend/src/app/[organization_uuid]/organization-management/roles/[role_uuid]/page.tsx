import OrgRoleSettings from "@/components/organization/organization-role";
import MainContainer from "@/shared/main-container";
import React from "react";

const RolesOrganizationManagement = () => {
  return (
    <MainContainer className="pt-0!">
      <OrgRoleSettings />
    </MainContainer>
  );
};

export default RolesOrganizationManagement;
